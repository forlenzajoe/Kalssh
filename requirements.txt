# Core
requests>=2.31
PyYAML>=6.0
python-dotenv>=1.0
numpy>=1.26
pandas>=2.1
scipy>=1.11

# Dashboard
streamlit>=1.30
plotly>=5.18

# Desktop app (native window wrapper; uses the built-in Edge WebView on Windows)
pywebview>=5.0

# Optional / extensible modeling (used only if available)
scikit-learn>=1.3

# Dev / test
pytest>=7.4
pytest-cov>=4.1
